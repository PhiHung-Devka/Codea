type Order = {
    orderId: number,
    userId: number,
    voucherId: number,
    totalPrice: number,
    date: Date,
    address: string,
    status: number,
    phone: string,
}

export type { Order };